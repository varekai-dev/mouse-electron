import React from "react";
import ReactDOM from "react-dom/client";
import TrayPopup from "./TrayPopup.tsx";
import "./index.css";

ReactDOM.createRoot(document.getElementById("root")!).render(
  <React.StrictMode>
    <TrayPopup />
  </React.StrictMode>
);

